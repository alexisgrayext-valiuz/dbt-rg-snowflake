version: 2

models:
  - name: test
    config:
      materialized: table
      database: FD_IO_DLK_RG
      schema: DLK_RG_BDA_ACQUISITION
      tags: daily

    description: Table POC snowflake/dbt

